import { ButtonHTMLAttributes } from 'react'

type TypeBtnType = {
  // readonly type?: 'button' | 'submit' | 'reset'
  readonly type?: JSX.IntrinsicElements['button']['type']
}

export default TypeBtnType
