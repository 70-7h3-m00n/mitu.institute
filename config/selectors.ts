type TypeSelectors = {
  readonly main: 'main'
  readonly programs: 'programs'
  readonly gtm: 'google-tag-manager'
  readonly sectionHero: 'section-hero'
}

const selectors: TypeSelectors = {
  main: 'main',
  programs: 'programs',
  gtm: 'google-tag-manager',
  sectionHero: 'section-hero'
}

export default selectors
